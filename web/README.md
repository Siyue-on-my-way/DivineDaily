# Divine Daily - 前端应用

基于 React + TypeScript + Vite 构建的占卜应用前端。

## 功能特性

- 🔮 周易占卜 - 传统易经智慧
- 🎴 塔罗占卜 - 西方神秘学
- 🤖 AI 解读 - 智能分析指导
- 📊 运势分析 - 每日运势预测
- 💾 历史记录 - 保存和查看占卜历史
- 📤 分享功能 - 分享占卜结果

## 技术栈

- **框架**: React 19 + TypeScript
- **构建工具**: Vite 5
- **路由**: React Router 7
- **状态管理**: Zustand
- **HTTP 客户端**: Axios
- **动画**: Framer Motion
- **样式**: CSS Modules + CSS Variables

## 环境要求

- Node.js >= 18.0.0
- npm >= 9.0.0

## 快速开始

### 1. 安装依赖

```bash
npm install
```

### 2. 配置环境变量

复制 `.env.example` 为 `.env.local` 并配置：

```bash
cp .env.example .env.local
```

编辑 `.env.local`：

```env
# API 代理目标地址
VITE_API_PROXY_TARGET=http://localhost:8080

# 其他配置...
```

### 3. 启动开发服务器

```bash
npm run dev
```

访问 http://localhost:5173

### 4. 构建生产版本

```bash
npm run build
```

### 5. 预览生产构建

```bash
npm run preview
```

## 项目结构

```
src/
├── api/              # API 接口定义
│   ├── auth.ts       # 认证 API
│   ├── divination.ts # 占卜 API
│   ├── fortune.ts    # 运势 API
│   └── config.ts     # 配置 API
├── components/       # 组件
│   ├── divination/   # 占卜相关组件
│   ├── mobile/       # 移动端通用组件
│   ├── ui/           # UI 基础组件
│   └── ErrorBoundary.tsx
├── hooks/            # 自定义 Hooks
│   ├── useToast.ts
│   └── useDivinationPolling.ts
├── lib/              # 工具库
│   ├── axios.ts      # Axios 配置
│   ├── AuthContext.tsx
│   └── env.ts        # 环境变量
├── pages/            # 页面组件
│   ├── HomePage.tsx
│   ├── DivinationPage.tsx
│   ├── TarotPage.tsx
│   ├── ProfilePage.tsx
│   ├── HistoryPage.tsx
│   └── AboutPage.tsx
├── store/            # Zustand 状态管理
│   ├── authStore.ts
│   ├── divinationStore.ts
│   └── uiStore.ts
├── styles/           # 全局样式
├── types/            # TypeScript 类型定义
└── App.tsx           # 应用入口
```

## 主要功能模块

### 认证系统

- 真实的 API 认证（替换了原来的 Mock）
- Token 自动注入和刷新
- 登录状态持久化

### 占卜流程

- 改进的轮询机制（避免内存泄漏）
- 支持组件卸载时自动清理
- 统一的错误处理

### 状态管理

使用 Zustand 管理全局状态：
- `authStore` - 用户认证状态
- `divinationStore` - 占卜结果缓存
- `uiStore` - UI 状态（Toast、Modal）

### 错误处理

- 全局错误边界
- Axios 拦截器统一处理
- Toast 通知替代 alert
- 开发/生产环境区分

## 开发指南

### 代码规范

项目使用 ESLint 进行代码检查：

```bash
npm run lint
```

### 类型检查

```bash
npm run type-check
```

### 路径别名

项目配置了路径别名，可以使用：

```typescript
import { Button } from '@components/mobile/Button'
import { useAuth } from '@lib/AuthContext'
import { divinationApi } from '@api/divination'
```

### 添加新页面

1. 在 `src/pages/` 创建页面组件
2. 在 `src/App.tsx` 添加路由
3. 如需导航，在 `MobileLayout.tsx` 添加导航项

### 添加新 API

1. 在 `src/api/` 创建 API 文件
2. 使用 `axiosInstance` 发起请求
3. 定义 TypeScript 类型

## 已修复的问题

✅ P0 阶段（已完成）：
- 实现真实的用户认证系统
- 添加 Axios 拦截器和统一错误处理
- 修复轮询机制的内存泄漏问题
- 修复底部导航栏不显示问题

✅ P1 阶段（已完成）：
- 添加全局状态管理（Zustand）
- 完善路由配置
- 实现真实的数据获取
- 创建 Toast 通知组件

✅ P2 阶段（已完成）：
- 修复 Textarea 字符计数同步问题
- 实现保存和分享功能
- 优化全局错误处理
- 添加环境变量配置
- 添加加载骨架屏
- 完善 TypeScript 配置

## 待优化项

- [ ] 集成 React Query 进行数据缓存
- [ ] 添加单元测试
- [ ] 性能优化（代码分割、懒加载）
- [ ] 可访问性改进
- [ ] PWA 支持

## 浏览器支持

- Chrome >= 90
- Firefox >= 88
- Safari >= 14
- Edge >= 90

## 许可证

MIT

## 联系方式

如有问题或建议，请联系：support@divinedaily.com
