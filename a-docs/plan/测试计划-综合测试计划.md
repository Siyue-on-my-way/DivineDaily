# DivineDaily 综合测试计划

**创建时间**: 2026-02-16  
**目标**: 建立完整的端到端测试流程，确保每次代码修改后系统功能正常

---

## 📋 测试目标

建立一套完整的自动化测试流程，覆盖：
1. **用户端业务流程** - 注册、登录、占卜（周易/塔罗）、每日运势、历史记录
2. **管理端业务流程** - LLM配置、Assistant配置、测试验证
3. **核心业务逻辑** - 意图识别、卦象生成、LLM调用、结果展示

---

## 🎯 测试范围

### 1. 用户端测试（User Flow）
- 用户注册与登录
- 用户档案管理
- 周易占卜（CN版本）
- 塔罗占卜（TAROT版本）
- 每日运势生成
- 历史记录查询

### 2. 管理端测试（Admin Flow）
- 管理员登录
- LLM配置管理（增删改查）
- LLM配置测试
- Assistant配置管理（增删改查）
- Assistant配置测试
- 测试用例管理

### 3. 核心业务逻辑测试
- 意图识别准确性
- 周易六爻算法
- 塔罗牌抽牌算法
- LLM调用与响应
- 结果格式验证

---

## 📊 测试架构

```
测试入口
  ├── 用户端测试套件
  │   ├── 认证测试
  │   ├── 档案测试
  │   ├── 占卜测试（周易）
  │   ├── 占卜测试（塔罗）
  │   ├── 运势测试
  │   └── 历史测试
  │
  ├── 管理端测试套件
  │   ├── 认证测试
  │   ├── LLM配置测试
  │   ├── Assistant配置测试
  │   └── 集成测试
  │
  └── 业务逻辑测试套件
      ├── 意图识别测试
      ├── 算法验证测试
      └── 数据一致性测试
```

---

## 🔧 测试环境配置

### 环境变量
```python
# 测试配置
BASE_URL = "http://8.148.26.166:48080"  # 后端API地址
API_BASE = f"{BASE_URL}/api/v1"

# 测试账号
ADMIN_USERNAME = "admin@163.com"
ADMIN_PASSWORD = "594120"

TEST_USER_EMAIL = "1441416954@qq.com"
TEST_USER_PASSWORD = "594120"
TEST_USER_NICKNAME = "测试用户"
```

### 依赖安装
```bash
pip install requests pytest pytest-asyncio httpx
```

---

## 📝 详细测试用例

### 测试套件 1: 用户认证流程

#### 1.1 用户注册
**API**: `POST /api/v1/auth/register`

**测试步骤**:
1. 发送注册请求（邮箱、密码、昵称）
2. 验证返回状态码 201
3. 验证返回包含 token、refresh_token、user 信息
4. 验证用户角色为 normal

**预期结果**:
```json
{
  "token": "eyJ...",
  "refresh_token": "eyJ...",
  "user": {
    "id": 1,
    "email": "test@example.com",
    "nickname": "测试用户",
    "role": "normal"
  }
}
```

#### 1.2 用户登录
**API**: `POST /api/v1/auth/login`

**测试步骤**:
1. 使用注册的账号登录
2. 验证返回状态码 200
3. 验证返回包含有效的 token
4. 保存 token 用于后续请求

#### 1.3 获取当前用户信息
**API**: `GET /api/v1/auth/me`

**测试步骤**:
1. 使用 token 请求用户信息
2. 验证返回的用户信息与登录时一致

#### 1.4 Token 刷新
**API**: `POST /api/v1/auth/refresh`

**测试步骤**:
1. 使用 refresh_token 刷新
2. 验证返回新的 access_token

---

### 测试套件 2: 用户档案管理

#### 2.1 创建用户档案
**API**: `POST /api/v1/profile`

**测试步骤**:
1. 提交用户档案信息（姓名、性别、生日、出生时间）
2. 验证返回状态码 200
3. 验证自动计算的农历、生肖、星座、八字信息

**请求示例**:
```json
{
  "name": "张三",
  "gender": "male",
  "birth_date": "1990-05-15",
  "birth_time": "14:30",
  "birth_location": "北京"
}
```

**预期结果**:
```json
{
  "id": 1,
  "name": "张三",
  "gender": "male",
  "birth_date": "1990-05-15",
  "lunar_date": "1990年四月廿一",
  "zodiac": "马",
  "constellation": "金牛座",
  "bazi": "庚午年 辛巳月 ..."
}
```

#### 2.2 获取用户档案
**API**: `GET /api/v1/profile`

#### 2.3 更新用户档案
**API**: `PUT /api/v1/profile`

#### 2.4 删除用户档案
**API**: `DELETE /api/v1/profile`

---

### 测试套件 3: 周易占卜流程

#### 3.1 周易占卜 - 决策类问题
**API**: `POST /api/v1/divinations/start`

**测试步骤**:
1. 提交决策类问题（包含"还是"关键词）
2. 验证意图识别为 binary_choice
3. 验证返回卦象信息
4. 验证返回 LLM 解读

**请求示例**:
```json
{
  "user_id": "1",
  "question": "我应该选择A公司还是B公司？",
  "version": "CN",
  "orientation": "E"
}
```

**预期结果**:
```json
{
  "session_id": "uuid-xxx",
  "outcome": "吉",
  "title": "乾卦",
  "summary": "此卦大吉...",
  "detail": "详细解读...",
  "hexagram_info": {
    "number": 1,
    "name": "乾卦",
    "upper_trigram": "乾",
    "lower_trigram": "乾",
    "changing_lines": [2, 5]
  }
}
```

#### 3.2 周易占卜 - 运势类问题
**测试步骤**:
1. 提交运势类问题（包含"运势"关键词）
2. 验证意图识别为 daily_luck
3. 验证路由到每日运势服务

#### 3.3 周易占卜 - 深度分析问题
**测试步骤**:
1. 提交复杂问题
2. 验证意图识别为 deep_analysis
3. 验证返回详细的卦象分析

---

### 测试套件 4: 塔罗占卜流程

#### 4.1 塔罗占卜 - 单张牌
**API**: `POST /api/v1/divinations/start`

**请求示例**:
```json
{
  "user_id": "1",
  "question": "我的转介绍会成功吗？",
  "version": "TAROT",
  "spread": "single"
}
```

**预期结果**:
```json
{
  "session_id": "uuid-xxx",
  "spread": "single",
  "cards": [
    {
      "name": "权杖十",
      "name_en": "Ten of Wands",
      "position": "当前",
      "is_reversed": false,
      "meaning": "负担、压力..."
    }
  ],
  "summary": "根据您抽出的牌面...",
  "detail": "详细解读..."
}
```

#### 4.2 塔罗占卜 - 三张牌阵
**请求示例**:
```json
{
  "version": "TAROT",
  "spread": "three",
  "question": "我的感情发展如何？"
}
```

**预期结果**: 返回3张牌（过去/现在/未来）

#### 4.3 塔罗占卜 - 十字牌阵
**请求示例**:
```json
{
  "version": "TAROT",
  "spread": "cross",
  "question": "我的事业发展？"
}
```

**预期结果**: 返回5张牌（当前/挑战/过去/未来/建议）

---

### 测试套件 5: 每日运势

#### 5.1 生成今日运势
**API**: `POST /api/v1/daily_fortune`

**测试步骤**:
1. 请求今日运势（不传日期参数）
2. 验证返回综合评分
3. 验证返回财运、事业、感情、健康
4. 验证返回幸运色、数字、方位、时辰
5. 验证返回宜忌事项
6. 验证返回节气、节日信息

**预期结果**:
```json
{
  "score": 85,
  "summary": "今日运势良好...",
  "wealth": "财运旺盛...",
  "career": "事业顺利...",
  "love": "感情和谐...",
  "health": "身体健康...",
  "lucky_color": "红色",
  "lucky_number": "8",
  "lucky_direction": "东方",
  "lucky_time": "午时",
  "yi": ["出行", "签约", "会友"],
  "ji": ["动土", "搬家"],
  "solar_term": "雨水",
  "festival": ""
}
```

#### 5.2 生成指定日期运势
**测试步骤**:
1. 传入 target_date 参数
2. 验证返回对应日期的运势

---

### 测试套件 6: 历史记录

#### 6.1 获取占卜历史
**API**: `GET /api/v1/divinations?limit=20&offset=0`

**测试步骤**:
1. 请求历史记录列表
2. 验证返回数据格式
3. 验证分页参数生效

**预期结果**:
```json
{
  "data": [
    {
      "id": "uuid-xxx",
      "question": "我应该...",
      "version": "CN",
      "outcome": "吉",
      "summary": "...",
      "created_at": "2026-02-16T10:00:00"
    }
  ],
  "total": 5
}
```

#### 6.2 获取单个占卜结果
**API**: `GET /api/v1/divinations/{session_id}`

---

### 测试套件 7: 管理员 - LLM 配置

#### 7.1 管理员登录
**API**: `POST /api/v1/auth/login`

**测试步骤**:
1. 使用管理员账号登录
2. 验证用户角色为 admin

#### 7.2 获取 LLM 配置列表
**API**: `GET /api/v1/configs/llm`

**测试步骤**:
1. 使用管理员 token 请求
2. 验证返回配置列表
3. 验证 API Key 已脱敏

#### 7.3 创建 LLM 配置
**API**: `POST /api/v1/configs/llm`

**请求示例**:
```json
{
  "name": "测试LLM",
  "provider": "openai",
  "url_type": "openai_compatible",
  "api_key": "sk-xxx",
  "endpoint": "https://api.openai.com/v1/chat/completions",
  "model_name": "gpt-3.5-turbo",
  "is_default": false,
  "is_enabled": true,
  "temperature": 0.7,
  "max_tokens": 2000,
  "timeout": 30
}
```

#### 7.4 更新 LLM 配置
**API**: `PUT /api/v1/configs/llm/{config_id}`

#### 7.5 删除 LLM 配置
**API**: `DELETE /api/v1/configs/llm/{config_id}`

**测试步骤**:
1. 尝试删除默认配置（应失败）
2. 尝试删除被使用的配置（应失败）
3. 删除未使用的配置（应成功）

#### 7.6 设置默认 LLM 配置
**API**: `POST /api/v1/configs/llm/{config_id}/set-default`

#### 7.7 测试 LLM 配置
**API**: `POST /api/v1/configs/llm/{config_id}/test`

**请求示例**:
```json
{
  "message": "你好，请用一句话介绍你自己",
  "mode": "block"
}
```

**预期结果**:
```json
{
  "success": true,
  "message": "测试成功",
  "request": {
    "user_message": "你好...",
    "model": "gpt-3.5-turbo",
    "endpoint": "https://..."
  },
  "response": "我是一个AI助手..."
}
```

---

### 测试套件 8: 管理员 - Assistant 配置

#### 8.1 获取 Assistant 配置列表
**API**: `GET /api/v1/configs/assistant`

#### 8.2 创建 Assistant 配置
**API**: `POST /api/v1/configs/assistant`

**请求示例**:
```json
{
  "name": "测试Assistant",
  "scene": "divination",
  "prompt_type": "answer",
  "question_type": "decision",
  "template": "你是一位专业的占卜师...\n\n用户问题：{{.question}}\n卦象：{{.hexagram}}",
  "llm_config_id": 1,
  "variables": [
    {"name": "question", "type": "string"},
    {"name": "hexagram", "type": "string"}
  ],
  "temperature": 0.7,
  "max_tokens": 2000,
  "timeout_seconds": 30,
  "is_enabled": true
}
```

#### 8.3 更新 Assistant 配置
**API**: `PUT /api/v1/configs/assistant/{config_id}`

#### 8.4 删除 Assistant 配置
**API**: `DELETE /api/v1/configs/assistant/{config_id}`

#### 8.5 获取测试用例
**API**: `GET /api/v1/configs/assistant/test-cases`

**预期结果**:
```json
{
  "data": {
    "测试Assistant": {
      "scene": "divination",
      "prompt_type": "answer",
      "question_type": "decision",
      "test_input": {
        "question": "我应该...",
        "hexagram": "乾卦"
      },
      "expected_keywords": ["建议", "分析"]
    }
  },
  "count": 1
}
```

#### 8.6 测试 Assistant 配置
**API**: `POST /api/v1/configs/assistant/{config_id}/test`

**预期结果**:
```json
{
  "success": true,
  "test_case": {...},
  "rendered_prompt": "渲染后的完整prompt",
  "llm_response": "LLM的回复",
  "validation": {
    "expected_keywords": ["建议", "分析"],
    "found_keywords": ["建议", "分析"],
    "missing_keywords": [],
    "keyword_match_rate": "2/2"
  }
}
```

---

## 🔄 完整测试流程

### 流程 1: 新用户完整体验流程
```
1. 用户注册 → 2. 用户登录 → 3. 创建档案 → 
4. 周易占卜 → 5. 查看结果 → 6. 塔罗占卜 → 
7. 查看结果 → 8. 每日运势 → 9. 查看历史
```

### 流程 2: 管理员配置流程
```
1. 管理员登录 → 2. 创建LLM配置 → 3. 测试LLM → 
4. 创建Assistant → 5. 测试Assistant → 6. 设置默认配置
```

### 流程 3: 端到端集成测试
```
1. 管理员配置LLM → 2. 管理员配置Assistant → 
3. 用户占卜 → 4. 验证使用了正确的配置 → 
5. 验证结果质量
```

---

## 📈 测试指标

### 成功标准
- ✅ 所有API返回正确的状态码
- ✅ 所有响应包含必需字段
- ✅ 数据格式符合预期
- ✅ 业务逻辑正确执行
- ✅ 错误处理正确

### 性能指标
- 登录响应时间 < 1s
- 占卜响应时间 < 30s
- LLM调用响应时间 < 20s
- 列表查询响应时间 < 2s

---

## 🛠️ 测试工具

### 自动化测试脚本
位置: `/mnt/DivineDaily/backend-python/tests/comprehensive_test.py`

### 手动测试工具
- Postman Collection
- Swagger UI: http://8.148.26.166:48080/docs

---

## 📝 测试报告模板

```
测试日期: 2026-02-16
测试人员: XXX
测试环境: 生产环境

测试结果:
- 用户端测试: ✅ 通过 (12/12)
- 管理端测试: ✅ 通过 (15/15)
- 业务逻辑测试: ✅ 通过 (8/8)

总计: 35/35 通过

发现问题:
1. 无

建议:
1. 增加性能测试
2. 增加并发测试
```

---

**维护者**: DivineDaily Team  
**版本**: v1.0

